{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/login", "dest": "/server.js" },
    { "src": "/api/weather", "dest": "/server.js" }
  ]
}
